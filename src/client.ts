type ClientConfig = {
  endpoint: string;
};

/**
 * Creates a new client to interact with the network API.
 */
const createClient = (config: ClientConfig) => {
  const client = null;
  return client;
};

export { createClient };
